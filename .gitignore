# build artifacts
/build/
*.o
*.obj
*.exe
*.elf
*.map
*.list
*.pdb
*.ilk

# editor / OS
Thumbs.db
.DS_Store
# Visual Studio のローカル状態（.suo/.ipch/Browse.VC.db 等）。生成物で肥大化するため非push。
.vs/

# CubeMX生成のmain.c（STMicroのテンプレート由来・コピーライト表記あり）は非公開に留める
src/host/stm32_l476/main.c

# 仕様書メモ（開発用ローカル・非push）。公開する変更履歴は change_history.md に分離済み。
script_spec_v*.md

# 作業規約メモ（Claude Code 用の恒久指示）はローカルのみ・非push。
CLAUDE.md

# 設計検討メモ（*.local.md）はローカルのみ・非push。実装前の技術メモ置き場。
*.local.md
